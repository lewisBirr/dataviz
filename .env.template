SAMPLE_VAR="blabliblub"

# Optional: HuggingFace token for authenticated access (higher rate limits).
# Obtain from https://huggingface.co/settings/tokens
# Leave empty or omit for anonymous streaming (public datasets still work).
# HF_TOKEN=
